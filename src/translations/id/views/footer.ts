import { parseIds } from "../../../helpers/translation";

const ids = {
  _path: ["footer"],
  address: "address",
  company: "company",
  email: "email",
  phone: "phone",
  zipCode: "zipcode",
};

export default parseIds(ids);
